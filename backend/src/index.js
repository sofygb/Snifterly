import app from "./app.js"; //importa la App
import './database.js'

app.listen(3001)
console.log("server listening on port 3001");

//npm start dev para ejecutar el proyecto
//npm run build para crea todo el codigo compilado o transpirado en dist